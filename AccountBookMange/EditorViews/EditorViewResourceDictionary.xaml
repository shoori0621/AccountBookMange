<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"         
                    xmlns:commons="clr-namespace:EditorViews.Commons"
                    xmlns:local="clr-namespace:EditorViews">
    
    <!--マイナスか検査する-->
    <commons:IsMinusConverter x:Key="IsMinusConverter"/>
    <!--絶対値に変換する-->
    <commons:AbsConveter x:Key="AbsConveter"/>

    <!--ベースカラー-->
    <SolidColorBrush x:Key="BaseColor" Color="YellowGreen"/>

    <!--項目タイトル-->
    <Style x:Key="ContentTitleLabel" TargetType="Label">
        <Setter Property="Width" Value="80"/>
        <Setter Property="Height" Value="25"/>        
    </Style>

    <!--ボタンベース-->
    <Style TargetType="Button" x:Key="BaseButtonStyle">
        <Setter Property="Width" Value="100"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Background" Value="{StaticResource BaseColor}"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        Name="border"
                        Height="{TemplateBinding Height}"
                        Width="{TemplateBinding Width}"
                        BorderThickness="1"
                        BorderBrush="DimGray"
                        Background="{TemplateBinding Background}">
                        <Label 
                            Name="label"
                            Content="{TemplateBinding Content}"
                            Foreground="{TemplateBinding Foreground}"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"         
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>